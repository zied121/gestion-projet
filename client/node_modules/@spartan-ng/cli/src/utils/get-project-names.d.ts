import { type Tree } from '@nx/devkit';
export declare const getProjectsAndNames: (tree: Tree) => {
    projects: Map<string, import("@nx/devkit").ProjectConfiguration>;
    projectNames: string[];
};
