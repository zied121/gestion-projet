"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.brainRadioHealthcheck = void 0;
const devkit_1 = require("@nx/devkit");
const generator_1 = require("../../migrate-radio/generator");
const healthchecks_1 = require("../healthchecks");
exports.brainRadioHealthcheck = {
    name: 'Brain Radio',
    async detect(tree, failure) {
        (0, devkit_1.visitNotIgnoredFiles)(tree, '/', (file) => {
            // if the file is a .ts or .htlm file, check for brain radio
            if (!file.endsWith('.ts') && !file.endsWith('.html')) {
                return;
            }
            const contents = tree.read(file, 'utf-8');
            if (!contents) {
                return;
            }
            if (contents.includes('<brn-radio')) {
                failure(`The <brn-radio> component is deprecated. Please use the <hlm-radio> instead.`, healthchecks_1.HealthcheckSeverity.Error, true);
            }
        });
    },
    fix: async (tree) => {
        await (0, generator_1.migrateRadioGenerator)(tree, { skipFormat: true });
        return true;
    },
    prompt: 'Would you like to migrate brain radio?',
};
//# sourceMappingURL=brn-radio.js.map