import type { NgZone } from '@angular/core';
import { Observable, Subject } from 'rxjs';
export declare function isElement(node?: Element | EventTarget | Node | null): node is Element;
export declare const createHoverObservable: (nativeElement: HTMLElement, zone: NgZone, destroyed$: Subject<void>) => Observable<boolean>;
